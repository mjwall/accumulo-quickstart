#!/bin/bash
# make sure your environment variables are set correctly, see
# accumulo-quickstart/install/bin/cloud-env.  You may also source
# that file from the command line first

# start hadoop
hd-start-all

# give HDFS time to come up
sleep 45

# start zookeeper
zk-start

# start accumulo
acc-start-all
