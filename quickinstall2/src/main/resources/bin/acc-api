#!/bin/bash

if [ "$(uname)" == "Darwin" ]; then
  CMD="open"
else
  CMD="xdg-open"
fi
echo "Accumulo API docs no longer shipped with the tarball, try http://accumulo.apache.org/1.6/apidocs/"
#TODO: download the source and build the javadocs for inclusion
#${CMD} $ACCUMULO_HOME/docs/apidocs/index.html
